export default function ProfilePage() {
  return (
    <main className="min-h-screen bg-background">
      <div className="mx-auto flex w-full max-w-3xl flex-col gap-6 px-4 py-12 sm:px-6">
        <h1 className="text-3xl font-semibold text-foreground">Profile settings</h1>
        <p className="text-sm text-muted-foreground">
          This is the profile settings page. We can build it out next.
        </p>
      </div>
    </main>
  )
}
